# Assistant Project Tracker

This file is internal working context for the coding assistant. Keep it concise, operational, and more technical than `NOTES.md`. Do not treat it as the user-facing project narrative.

## Workspace state

- Workspace: `oncology-target-discovery`
- Current artifacts: `oncology-target-discovery-project-plan.md`, `NOTES.md`, `CLAUDE.md`, `case_study_research.md`, `environment.yml`, `.gitignore`, `src/data_utils.py`, `scripts/download_tcga.py`, `scripts/download_gtex.py`.
- Git repository initialized (branch `main`) and pushed to `https://github.com/tuhinc5203/oncology-target-discovery` (`origin/main` tracked). Safe to use normal git commands, including push.
- Local conda environment `target-discovery` (Python 3.10) created with pandas, numpy, scipy, matplotlib, seaborn, lifelines, jupyter, ipykernel. Jupyter kernel registered as "Python (target-discovery)". Exact versions pinned in `environment.yml`.
- Directory layout: `data/raw/{depmap,tcga,gtex,hpa}/` (untracked except `.gitkeep`, for original downloads), `data/processed/` (untracked except `.gitkeep`, for cleaned/intermediate data), `notebooks/`, `src/`, `scripts/`.
- `data/raw/depmap/` populated: `Model Data.csv` (2,154 × 49) and `CRISPR Gene Effect.csv` (1,208 cell lines × ~18,531 genes), DepMap Public **26Q1**.
- `data/raw/tcga/` populated: `BRCA.rnaseqv2_RSEM_genes_normalized.data.txt` (20,531 genes × 1,212 samples), `All_CDEs.txt` (131 attrs × 1,097 patients, has ER/PR/HER2 IHC/FISH status), `BRCA.clin.merged.picked.txt` (survival fields). Source: GDAC Firehose `stddata__2016_01_28` BRCA run. Downloaded via `scripts/download_tcga.py` (verified byte-identical on a fresh run — no bot-check on this host, unlike DepMap).
- `data/raw/gtex/` populated: `GTEx_Analysis_v11_gene_median_tpm.gct.gz` (74,628 genes × 68 tissues), GTEx **v11**. Downloaded via `scripts/download_gtex.py` (public GCS bucket, no auth).
- `data/raw/hpa/` populated: `proteinatlas.tsv` (20,162 genes × 118 columns — protein class, subcellular location, per-cancer-type HPA-computed survival prognostics), HPA **25.1** (Ensembl 109). Downloaded directly (`proteinatlas.org/download/proteinatlas.tsv.zip`, no auth, no bot-check).

## User-facing source of truth

- `NOTES.md` is for the user: plain-language catch-up, weekly tasks, decisions, outcomes, and material that can later become `README.md`.
- `oncology-target-discovery-project-plan.md` is the detailed reference plan.
- Keep implementation details, unresolved choices, and assistant resume state here instead of cluttering `NOTES.md`.

## Scientific scope

- Disease focus: TNBC.
- Public sources: DepMap Chronos, TCGA-BRCA, GTEx, and Human Protein Atlas.
- TNBC must be defined explicitly in both cell-line and patient analyses. Never silently substitute all breast cancer for TNBC.
- Prior Oxford BioTherapeutics experience is context only. Never request, infer, or record proprietary targets, results, methods, or confidential information.

## Current implementation state

- Week 0 is complete: TNBC decision recorded, environment/kernel/git set up, DepMap files downloaded and inspected (26Q1 release — see below).
- DepMap's public data (Chronos CRISPR scores, cell line metadata) requires no account; downloaded directly from the portal's "All Data"/"Custom Downloads" tabs (no longer bulk-published to Figshare as of 25Q2).
- **DepMap TNBC cell-line mapping resolved:** `Model Data.csv`'s `ModelSubtypeFeatures` column directly labels molecular subtype (e.g. `basal_A TNBC`, `luminal TNBC`, `TNBC`) for most `OncotreeLineage == "Breast"` rows. Filtering for substring `"TNBC"` gives 34 of 96 breast cell lines; 25 of those have CRISPR data in `CRISPR Gene Effect.csv`. 15 breast lines have no `ModelSubtypeFeatures` value — treat as unknown, exclude from both TNBC and non-TNBC comparison groups, don't default them to "non-TNBC." No manual receptor-status mapping needed. Full reasoning in `NOTES.md`'s "Defining the TNBC cell-line set" section.
- **TCGA-BRCA TNBC patient mapping resolved:** `data_utils.get_tnbc_patient_barcodes()` combines `breast_carcinoma_estrogen_receptor_status`, `..._progesterone_receptor_status`, and HER2 IHC (`lab_proc_her2_neu_immunohistochemistry_receptor_status`) + FISH (`lab_procedure_her2_neu_in_situ_hybrid_outcome_type`) from `All_CDEs.txt`. HER2-negative = IHC negative, OR IHC equivocal with FISH negative (ASCO/CAP reflex rule). TNBC = ER-neg AND PR-neg AND HER2-neg; missing/indeterminate on any marker excludes the patient from both groups. Result: 143/1,097 patients (13%, matches expected clinical prevalence), 142 with matched tumor expression data. Full reasoning in `NOTES.md`'s "Defining TCGA-BRCA's TNBC patient set" section.
- **`src/data_utils.py` now has loaders for all 3 downloaded sources:** `load_model_metadata`/`load_gene_effect`/`get_tnbc_model_ids` (DepMap), `load_tcga_expression`/`load_tcga_clinical_cdes`/`get_tnbc_patient_barcodes`/`tcga_patient_barcode`/`tcga_is_tumor_sample` (TCGA), `load_gtex_median_tpm` (GTEx). All verified against the real files.
- **Week 1 is complete.** `notebooks/01_data_eda.ipynb` executed end-to-end (via `jupyter nbconvert --execute`, no errors): dimensions/missingness/distributions for all 3 downloaded sources + the clinical CDE table. Key numbers: DepMap 3.96% missing (concentrated in ~8% of genes); TCGA expression 0% missing, heavily right-skewed (log2(x+1) planned); GTEx 0% missing, 54% exact zeros. Full findings + explanations in `NOTES.md`'s "EDA findings worth remembering" section. TCGA matched-normal-vs-GTEx-baseline decision still open (1,093 tumor vs. 119 normal/other samples in the expression matrix).
- **Week 2 is complete**, including the biology plausibility check. `notebooks/02_selective_essentiality.ipynb` runs Welch's t-test AND Mann-Whitney U per gene (25 TNBC lines vs. 1,181 other DepMap lines with CRISPR data; 2 unknown-subtype breast lines excluded from both groups), BH-FDR corrected. Tests correlate strongly overall (Spearman ρ=0.87) but only 40% overlap in genes significant at q<0.10 — Mann-Whitney used as primary (group-size imbalance reasoning). **The raw statistical shortlist (`depmap_tnbc_shortlist.csv`) turned out to be contaminated** with (a) pan-essential housekeeping genes (`SNRPF`, `LSM2`, `POLR3A`, `SMU1`, `CCT3`, `PSMA5` — pan-cancer mean Chronos effect below -1.0, essential in every cell line, not TNBC-specific) and (b) a likely sex-chromosome artifact (`AMELY`, Y-linked, TNBC group is 100% female vs. majority-male comparison group, zero expression in real TNBC tumors). Fixed by adding two filters — pan-cancer mean effect > -1.0, and real TCGA-BRCA TNBC-tumor expression (median log2(RSEM+1) > 1.0, matched via **Entrez ID** not symbol, since ~180 DepMap gene symbols have drifted from this 2016-vintage TCGA file's naming) — producing `data/processed/depmap_tnbc_shortlist_v2.csv`, **the one to use going forward, not the original**. Literature-checked 5 of the new top candidates: `LY6E` has strong independent TNBC-specific published support (different method entirely — RNA-seq/protein/xenograft); `CDKN1A`, `ZFX`, `BIRC7` have real but more general breast-cancer support; `KIF2C` unconfirmed either way. Full write-up in `NOTES.md`'s "The biological plausibility check found a real problem" section.
- **Week 3 is complete.** `notebooks/03_tumor_selectivity_safety.ipynb` adds tumor-vs-matched-normal-breast fold change (TCGA-BRCA, 1,093 tumor vs. 112 normal samples, BH-FDR corrected) and a GTEx critical-tissue (heart/liver/whole-blood-as-bone-marrow-proxy) safety flag to every Week 2 candidate → `data/processed/depmap_tnbc_week3_final.csv`. Resolved the standing "TCGA normal vs. GTEx baseline" open decision: not either/or — TCGA matched-normal used for tumor selectivity (same-study comparability), GTEx used for safety (only source with non-breast organs). **Standout candidate: `KIF2C`** — massively tumor-overexpressed (log2fc +3.35, q<1e-58), low critical-tissue expression (max 1.8 TPM), and independently published as breast-cancer-overexpressed/prognostic (checked via literature search, not memory) — three independent lines of evidence converging. Real safety flags on `IFI6`/`LY6E` (high whole-blood expression, likely interferon-gene circulating-immune-cell signal) and `H2AC6` (256 TPM critical-tissue max, likely general-proliferation histone signal, not TNBC-specific). `SIGLEC8`'s apparent tumor-overexpression flagged as a probable tumor-infiltrating-immune-cell artifact (eosinophil/mast-cell marker), not real cancer-cell biology — a reminder bulk RNA-seq can't separate the two.
- **HPA druggability check added (was wrongly auto-skipped as "optional" the first time — caught and fixed).** `load_hpa()` added to `data_utils.py`. `KIF2C`, `CDKN1A`, `ZFX`, `H2AC6`, `BIRC7` are all `Predicted intracellular proteins` (mostly nuclear) — no antibody approach, would need a small molecule (kinesin inhibitors like KIF11/Eg5 are a real precedent for `KIF2C` specifically). `LY6E` is `Predicted membrane proteins` — more tractable. **Real discrepancy investigated, not glossed over:** HPA's own Kaplan-Meier analysis calls `KIF2C` "unprognostic" in breast cancer (p=0.0714 TCGA, p=0.0164 validation) despite the strong published literature found earlier. Resolved: HPA uses a strict p<0.001 cutoff (confirmed via their methods docs) specifically because they screen genome-wide — a p=0.0164 is real, just doesn't clear that conservative single-cohort bar, whereas the published meta-analyses pooled many cohorts for more power. Every other candidate also came back "unprognostic" by this same strict standard — treat HPA's label as a high bar, not proof of no association. Full write-up in `NOTES.md`'s "HPA" section.
- **Week 4 complete: survival analysis + composite scoring.** Narrowed Week 3's 40 candidates to 15 first: filtered to `log2fc_tumor_vs_normal > 0` (20 genes), ranked by combined essentiality-q + tumor-selectivity-q rank, took top 15 → `data/processed/depmap_tnbc_week4_candidates15.csv`. New `get_tnbc_survival_data()` in `data_utils.py` builds `time`/`event` (+ age, ordinal I–IV stage) from `All_CDEs.txt`. `notebooks/04_survival_analysis.ipynb`: KM + log-rank per gene (median split) — none survive FDR correction (only 21 deaths among 142 patients, low power). Cox regression (continuous expression, age/stage-adjusted): **`LY6E` significant after adjustment** (HR 1.74, p=0.0024, q=0.036) — adjusting *strengthened* the signal, a good sign it's not confounded. `KIF2C` shows no survival signal here (p=0.72), consistent with — not contradicting — Week 3's HPA finding that a single cohort lacks power to detect its published (multi-cohort, pooled) survival association. Results: `data/processed/depmap_tnbc_week4_survival.csv`. **Composite score** (see "Composite score (Week 4)" under Resolved decisions) added in the same notebook's final section → `data/processed/depmap_tnbc_final_ranked_targets.csv`, the project's final ranked target list.
- **Reproducibility bug found, root-caused, and fixed at the actual cause (not patched around): notebooks 01-03 were executing under the base conda env, not `target-discovery`.** Root cause chain, confirmed step by step: (1) notebooks were built via `nbformat` without ever getting `kernelspec` metadata, so `nbconvert` fell back to a kernel literally named `"python3"`; (2) every conda env with `ipykernel` installed auto-registers its own `"python3"` kernel whose launch command is the **bare word `"python"`**, not a full path (unlike the deliberately-registered `target-discovery` kernel, which uses a full path and was never affected); (3) a bare `"python"` resolves via the **calling shell's `PATH` at kernel-launch time**, not via which Python ran `nbconvert` or where the kernelspec file lives — and this shell's `PATH` has base conda ahead of `target-discovery` (the same underlying issue as the `conda activate` PATH gotcha below, now shown to reach further than manual commands). Discovered when notebook 04 failed on `import lifelines` (not in base env). **Verified no damage:** re-ran notebooks 01-03 with the kernel explicitly forced and diffed every output against what was committed — identical (one harmless one-time "building font cache" message aside; the regenerated `data/processed` CSVs differ only at ~15 decimal digits, confirmed by diff, from numpy/scipy version differences). **Fixed at the root, verified without relying on a flag:** every notebook now has explicit `kernelspec` metadata pointing at the `target-discovery` kernel (the full-path one) saved directly in the `.ipynb`. Confirmed this is sufficient on its own — ran a disposable copy of notebook 04 via `nbconvert --execute` with **zero kernel-related flags** and it still correctly launched Python 3.10.21 (checked `language_info.version` in the output, and that `lifelines` imported). All 4 notebooks spot-checked directly (`language_info.version` == `3.10.21`, `kernelspec.name` == `target-discovery`) — not assumed. Lesson: a package being in `environment.yml` doesn't prove a notebook run actually used it — check which kernel actually executed, and don't trust a CLI flag as the permanent fix when the notebook's own saved metadata can be.
- **Local dev gotcha:** `conda activate target-discovery` doesn't reliably win the `PATH` race in this shell — call the env's Python by full path (`/Applications/miniconda3/envs/target-discovery/bin/python3`) to avoid silently running without the installed packages. Precisely verified what each bare command actually resolves to in this shell, since it's easy to get wrong: `python3` → `/opt/homebrew/bin/python3` (Homebrew's system Python), while plain `python` (no "3") → `/Applications/miniconda3/bin/python` (**base** conda, not `target-discovery` — Homebrew doesn't provide an unversioned `python`, base conda does). This second one is exactly what caused the notebook-kernel bug above: an auto-generated kernelspec's bare `"python"` command resolved to base conda via this same `PATH` ordering. Same underlying category of problem (PATH doesn't prioritize `target-discovery`), but two different wrong interpreters depending on which exact command name gets resolved — don't assume they're the same failure.
- **Bot-check pattern, confirmed twice now:** DepMap's portal blocks automated requests (Cloudflare) even though no login is actually required. TCGA/GTEx don't have this — GDAC Firehose and GTEx's GCS bucket are plain public HTTP with no bot-check, so those two are scripted (`scripts/download_tcga.py`, `scripts/download_gtex.py`), while DepMap stays a manual step. Don't assume a data portal is blocked just because DepMap was — test each one directly (`curl`/`WebFetch`) before concluding it needs the user's browser.

- **Week 5 case-study drafts written** in `NOTES.md` ("Case studies: four targets, four different kinds of evidence", under Week 5): comparison table + one section each for `IFI6`/`LY6E`/`HPRT1`/`KIF2C` + cross-cutting takeaways, README-ready. Each target's "Next experimental step" now has an assistant-written **draft** (user asked for them; labeled as drafts to be edited in the user's own voice; `case_study_research.md` placeholders still blank). Not yet committed. Remaining Week 5: user's next-step text, README, summary visualization, notebook cleanup, optional Streamlit.

## Resume procedure

1. Read this file, `NOTES.md`, and the relevant Week 0/Week 1 section of the project plan.
2. Check whether the user has downloaded DepMap files or provided access details.
3. Establish the local Python environment before writing analysis code.
4. Inspect real file schemas before choosing loaders or subtype filters.
5. Record dataset release/date and mapping decisions in both code metadata and `NOTES.md`'s "Technical concepts & decisions" section.
6. After each meaningful milestone, update the completed task in `NOTES.md` and the implementation state here.

## Technical guardrails

- Prefer small, reproducible modules over notebook-only logic.
- Preserve raw downloads separately from cleaned/intermediate data.
- `data/processed/` is gitignored by default (like `data/raw/`), but small, meaningful, named result files worth tracking in git history (e.g. a shortlist CSV a later week's analysis depends on) get `git add -f`'d individually rather than left uncommitted — same treatment as the `.gitkeep` placeholders. Don't force-add large/disposable intermediate caches this way.
- Record missingness, sample counts, identifiers, and filtering decisions.
- Use multiple-testing correction for genome-wide comparisons.
- Validate candidate rankings against known TNBC biology before presenting novel hypotheses.
- Do not claim a target is validated; this project produces computational prioritization for follow-up experiments.

## Open decisions

None currently open. Week 4's composite-score weighting (previously paused) was resolved with the user, and Week 5's case-study target selection is resolved — see "Resolved decisions" below.

## Resolved decisions

- **DepMap release:** Public 26Q1 (`Model Data.csv`, `CRISPR Gene Effect.csv`).
- **DepMap TNBC cell-line mapping:** substring match on `ModelSubtypeFeatures` containing `"TNBC"`, among `OncotreeLineage == "Breast"` rows. 34 lines match; 25 have CRISPR data.
- **TCGA-BRCA run:** GDAC Firehose `stddata__2016_01_28` (BRCA) — same source as cBioPortal's `brca_tcga` study.
- **TCGA-BRCA TNBC patient mapping:** ER-neg AND PR-neg AND combined-HER2-neg (IHC negative, or IHC equivocal + FISH negative), via `get_tnbc_patient_barcodes()`. 143/1,097 patients; 142 with expression data.
- **GTEx release:** v11 (`GTEx_Analysis_v11_gene_median_tpm.gct.gz`, 74,628 genes × 68 tissues).
- **Essentiality test + effect size:** mean-difference effect size; Mann-Whitney U as primary test (t-test kept alongside for comparison).
- **Essentiality shortlist plausibility filters:** pan-cancer mean Chronos effect > -1.0 (excludes common-essential genes), and median log2(RSEM+1) > 1.0 in real TCGA-BRCA TNBC tumors, matched by Entrez ID. Use `depmap_tnbc_shortlist_v2.csv`, not the unfiltered `depmap_tnbc_shortlist.csv`.
- **TCGA-normal-vs-GTEx-baseline:** not either/or — TCGA matched-normal breast (112 samples) for tumor selectivity, GTEx for safety across other organs. Critical tissues checked: `Heart_Left_Ventricle`, `Heart_Atrial_Appendage`, `Liver`, `Whole_Blood` (bone-marrow proxy). Safety flag thresholds: <5 TPM low, 5–20 moderate, >20 high.
- **HPA release:** 25.1 (Ensembl 109), `proteinatlas.tsv`. Prognostic calls use HPA's own strict p<0.001 threshold — don't read "unprognostic" as "no real association," see `NOTES.md`.
- **15-candidate narrowing for Week 4:** hard filter on `log2fc_tumor_vs_normal > 0`, then top 15 by combined essentiality+tumor-selectivity q-value rank. Full 40 not used for survival testing.
- **Survival analysis method:** KM + log-rank (median split) for visualization; Cox PH on continuous log2 expression (unadjusted + age/stage-adjusted) as the primary test, since it doesn't waste information the way a median split does.
- **Composite score (Week 4):** rank-average of three continuous evidence axes only — essentiality `u_q`, tumor selectivity `tumor_vs_normal_q`, survival `cox_q_adj` — each equally weighted (user's explicit choice: "averaging the three evidence strength makes sense"). Safety (`safety_flag`) and druggability (HPA `Protein class`/`Subcellular main location`) are kept as annotation columns, not blended into the score — user's explicit choice, adjustable later; matches literature precedent (Open Targets keeps a separate tractability/safety panel; a PAK2/HNSCC DepMap paper uses safety as a pre-ranking filter, never a score input; shinyDepMap avoids single composite scores for anti-correlated axes entirely). Implemented in `notebooks/04_survival_analysis.ipynb`'s final section → `data/processed/depmap_tnbc_final_ranked_targets.csv`. Result: `IFI6` ranks 1st (consistent across all three axes, never a standout), `LY6E` 2nd (the one axis with real significance — survival q=0.036 — but a middling tumor-selectivity rank drags its average down). Both carry "high" safety flags, which is the concrete argument for annotation-over-filter: a hard safety gate would have removed 2 of the top 3 candidates before scoring.
- **5-axis comparison (side-by-side only, not the project's result):** same notebook, final section — folds safety (`gtex_critical_tissue_max_tpm` rank, continuous not bucketed) and druggability (new 3-tier rule on HPA `Protein class`: FDA-approved target > potential target/membrane protein > other) in as two more equal ranks. User explicitly requested this as "something to see in comparison," not a replacement. Result → `data/processed/depmap_tnbc_5axis_comparison.csv`: `LY6E` drops 2nd→tied 5th (safety penalty from a likely whole-blood immune-cell artifact outweighs its real survival significance — the concrete case for keeping safety out of the primary score); `HPRT1`/`CREB3L4` rise into the top tier mainly via tractability rather than disease evidence (`HPRT1` has the worst essentiality rank of all 15 but is already FDA-approved); `IFI6` stays #1 regardless.
- **Week 5 case-study targets:** user picked `IFI6`, `LY6E`, `HPRT1`, `KIF2C` (four, not the plan's suggested one–two). `IFI6`/`LY6E` are the composite-score top 2; `HPRT1` (composite rank 5) is included as the project's tractability-vs-evidence contrast case, on the strength of HPA's `FDA approved drug targets` protein-class label despite the worst essentiality rank of the 15 candidates. **Note: `HPRT1` is not allopurinol's target** (that's xanthine oxidase; an earlier note here got this wrong) — the real FDA-approved-drug connection is that `HPRT1` activates thiopurine prodrugs (6-mercaptopurine/6-thioguanine/azathioprine), not that a drug inhibits `HPRT1` itself. `KIF2C` (composite rank 9) was added after rechecking the Week 2 essentiality filter, which only ever tested differential essentiality (TNBC vs. other lines) and never absolute dependency magnitude — rechecked directly against this project's own 25-line TNBC set, `KIF2C` has by far the strongest real dependency (median Chronos effect -0.487, 80% of TNBC lines dependent at ≤-0.30) versus `IFI6`/`LY6E`/`HPRT1` (medians -0.046/-0.037/-0.030, ≤12% of lines). Literature/druggability research for `IFI6`, `HPRT1`, and `KIF2C`'s addition (LY6E already covered in Weeks 2-3) is in `case_study_research.md`, new project artifact for Week 5.
